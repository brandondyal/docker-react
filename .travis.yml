sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t brandondyal/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true brandondyal/docker-react npm run test
